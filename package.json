{
  "name": "holiday-calendar-ts",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r run build",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "typecheck": "pnpm -r run typecheck"
  },
  "volta": {
    "node": "24.19.0",
    "pnpm": "11.21.0"
  },
  "packageManager": "pnpm@11.21.0",
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@vitest/coverage-v8": "^3.0.0",
    "npm-snapshot": "^1.1.2",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "pnpm": {
    "ignoredBuiltDependencies": [
      "esbuild",
      "msw"
    ]
  }
}
